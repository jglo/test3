# Configuration for News to X Post Generator Bot

# Number of posts to generate (default: 5)
NUM_POSTS=5

# Use demo mode with sample headlines (default: false)
# Set to 'true' to use demo headlines instead of fetching from RSS
DEMO_MODE=false
